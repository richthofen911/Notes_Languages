::原型声明
  在使用函数之前，C++编译器必须知道函数的参数类型和返回值类型，所以必须写原型
  例子
    double sqrt(double);
  以分号结尾表明是一个原型语句，而不是函数头（函数头后面要定义函数体）
  
::调用
  C++中，函数调用必须包括括号，即使没有参数
  
::在多函数程序中使用using编译指令
  当前通行的理念是，只让需要访问命名空间(以std为例）的函数访问它是最好的选择。例如在有main()函数和custom1()
  函数，窃只有main()函数使用的cout涉及到std，则应把using放在main()函数中，使得只有该函数能访问命名空间std.
  另外，也建议，如果只用到cout，应该使用using std::cout; 而不是using namespace std;
  
    
